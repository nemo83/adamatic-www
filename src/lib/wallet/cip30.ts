/**
 * CIP-30 helpers. Empty in Phase B — Phase D will populate this with
 * `listInstalledWallets()` enumerating `window.cardano.*` and the
 * friendly-label override map (Eternl, Nami, Lace, Typhon, Vespr, NuFi).
 */
export {};
