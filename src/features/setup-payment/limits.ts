/**
 * Hard caps for the beta. Surfaced both in the form (validation / UI gates)
 * and in the LimitsStrip header so users see them up front.
 */
export const MAX_PULLS = 5;
export const MAX_WALLETS = 2;
